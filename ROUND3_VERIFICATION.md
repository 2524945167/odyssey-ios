# 第 3 轮补修验收记录

状态：本地补修，待 CI 编译与测试、待真机最终验证。未进入第 4 轮。

## 修改范围

- 格式仅保留 Responses API、Chat Completions API、Anthropic API（Messages）。
- 旧 `openai_compatible` 加载时映射到 Chat Completions，保留地址、模型与密钥；显式保存后写入新标识。未知格式解码抛错，不默认选择其他协议。
- 配置 Store 在主线程发送 ObservableObject 更新通知，设置页实际订阅该通知；保存或清除后重新读取真实存储状态。
- 按用户最新确认，移除独立系统弹框；点击清除按钮后在其下方原位展开说明、“取消”和“确认清除”。两个按钮各自使用 borderless 样式，避免 Form 同行按钮共享整行点击。取消仅收起区域；确认才删除，且拒绝未展开或重复的确认回调。
- 隔离 UserDefaults 测试通过 Store 保存假密钥，并解码实际配置 Data 检查字段白名单与假密钥未落盘。

## 自动化证据边界

- 格式往返与迁移测试：覆盖三种稳定标识、旧格式迁移、字段和密钥保留、未知格式拒绝。
- 更新通知测试：检查保存和清除的通知及摘要值，不等同于真实屏幕刷新测试。
- 清除回调测试：检查取消零次删除、重新进入仍保存密钥、确认一次删除以及重复确认不重复删除；不等同于真实点击测试。
- ImageRenderer：仅检查静态浅深色渲染，焦点绑定关闭；不证明键盘和原位确认区域交互正确。原位确认区域不受焦点开关控制。
- Keychain 更新失败测试使用 Mock；不等同于真实系统钥匙串故障注入。
- 当前 Windows 环境不能执行 Xcode/iOS 测试；未经本次 CI 验证，不声明测试通过、零警告或新 IPA 已生成。

## 待真机验收（iPhone 16 Pro）

请使用测试凭据，不必发送真实 API Key 或含密钥截图。

1. 格式菜单只有三个已确认名称；各格式均可填写自定义 HTTPS 地址和 Model ID。
2. 保存配置返回设置页，摘要立即更新；再次修改模型保存返回，摘要再次更新。
3. 已保存密钥时，点清除：确认区域紧接按钮下方展开，无独立弹框。点取消：区域收起且保存状态不变；离开再进入、重启 App 后仍显示已保存。确认取消按钮不会误触发确认清除。
4. 点清除 → 确认：密钥状态变为未保存，Base URL、Model ID、格式不变，设置摘要变为未配置。
5. 原文输入框可正常聚焦，点上方空白可收起键盘；配置表单输入、保存、原位确认正常；检查浅深色及较大字号，说明和两个操作不能截断或重叠。
6. 从旧版带自定义地址的兼容格式配置升级后，显示 Chat Completions API，地址、模型和保存密钥状态保留。

CI 成功后补记 Commit、运行链接、测试统计、警告检查结果和内部 IPA SHA-256。真机通过前 README 不标记完成。
